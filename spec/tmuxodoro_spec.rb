require "spec_helper"

describe Tmuxodoro do
  it "has a version number" do
    expect(Tmuxodoro::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
